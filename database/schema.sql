CREATE TABLE roast_bot_table (
    id INT PRIMARY KEY AUTO_INCREMENT,
    username  TEXT         NOT NULL,
    userID    TEXT         NOT NULL,
    userXP    VARCHAR(100) NOT NULL,
    userLevel VARCHAR(100) NOT NULL
);