CREATE TABLE roast_bot_custom_roast (
    id INT PRIMARY KEY AUTO_INCREMENT,
    username     TEXT NOT NULL,
    userID       TEXT NOT NULL,
    roastNumber  TINYINT NOT NULL,
    roasts       TEXT NOT NULL
);